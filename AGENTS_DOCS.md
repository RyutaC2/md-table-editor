# Markdown Grid Editor リポジトリ仕様書

この文書は、VS Code 拡張機能 `Markdown Grid Editor` の仕様、設計意図、実装状態を AI が把握するための一次資料です。作業開始時に必ず確認し、コードと記述が一致しない場合は同じ変更内で更新してください。

## プロジェクト概要

Markdown の表は、パイプ、区切り行、列揃えを人間が維持しながら編集する必要があり、規模が大きくなるほど負担が増えます。本拡張機能は、VS Code 内で Markdown ソースと Excel ライクな GUI を並べて表示し、Markdown 構文を直接操作しなくても表を作成・編集できるようにします。

- Marketplace 表示名: `Markdown Grid Editor`
- 拡張機能 ID: `md-table-editor`
- publisher: `RyutaC2`
- 初回リリース: `0.1.0`
- 現在のバージョン: `0.2.1`
- ライセンス: MIT
- 対応環境: VS Code Desktop（Windows、macOS、Linux）および Web（vscode.dev、github.dev）
- 表示言語: 日本語、英語。その他の言語では英語へフォールバックします。
- テレメトリー: 収集しません。

## 要件定義と実装承認

ユーザーへのヒアリングによる初回リリースの要件定義は完了しています。ユーザーから実装開始の明示的な承認を得ているため、以降は本仕様を基準に実装します。仕様を大きく変更する必要が生じた場合は、変更理由と選択肢をユーザーへ提示して再確認します。

## 0.2.0 確定・実装済み仕様

2026-08-11に、挿入、行番号、ツールバー、選択・移動、ファイル入出力を刷新する仕様が承認され、同日0.2.0として実装しました。以下はすべて実装済みの仕様です。

### テーブル挿入

- Markdownエディターのコンテキストメニューへ `クイックテーブル挿入` / `Quick Insert Table` と `テーブル挿入` / `Insert Table` を表示します。
- クイック挿入は2列×2行、すなわち0行目のMarkdownヘッダーと1行目のデータ行を持つ空表を確認なしで挿入します。
- 通常挿入は行数、列数の順にQuick Pickを表示し、それぞれ1～20から選択します。行数にはMarkdownヘッダーを含み、区切り行を含みません。
- どちらも現在の挿入位置判定、空行、即時GUI表示、左上セルの編集開始を維持します。

### 行番号、行列選択、並べ替え

- Markdownヘッダーの行番号を0、本文を1、2、3…と表示します。内部の0始まり行インデックスと画面表示を一致させます。
- 左上角をクリックすると表全体を選択します。
- 未選択の行・列見出しから1回目のドラッグを行うと、行は縦方向、列は横方向だけに連続範囲を選択します。
- 選択済みの連続した行・列見出しを再度ドラッグすると、そのブロックを並べ替えます。移動元の半透明表示、ドラッグプレビュー、挿入位置ライン、ヘッダー行固定を維持します。
- 複数行・複数列を選択中の追加操作は選択数と同じ数を一括追加し、削除操作は選択対象を一括削除します。Markdownヘッダーと最後の1列は削除しません。

### セル範囲移動

- 連続した矩形の複数セル選択には、選択範囲全体を囲む枠線を表示します。
- 枠線をドラッグすると、矩形の大きさと相対配置を保ってセルのMarkdown生文字列を移動します。移動元は空にし、移動先へ上書きします。
- 移動先が表の上端、下端、左端、右端を超える場合は、その方向へそれ以上移動させません。自動的な行列追加やデータの切り捨ては行いません。
- 不連続選択はセル移動の対象外とし、通常のコピー・貼り付け規則を維持します。

### ツールバーとズーム

- 十分な幅では1段表示とし、左から貼り付け、切り取り、コピー、セル内容削除、Undo、Redo、インポート、エクスポート、ズーム、全列の指定なし・左・中央・右配置、行操作3個、列操作3個の順に置きます。
- 貼り付け、切り取り、コピー、削除、Undo、Redo、インポート、エクスポート、配置はアイコンのみとし、`title` と `aria-label` を必須とします。十分な幅では行・列操作だけアイコンと名称を併記します。
- 幅が狭くなると、アイコンのみ1段、指定グループ順のアイコンのみ2段、一部省略した2段の順に切り替えます。
- 最狭幅では左端に展開メニューを追加し、インポート、エクスポート、ズームだけを格納します。配置、行操作、列操作は直接表示したままとし、ツールバーにスクロールバーを表示しません。
- ズームはツールバーを固定したまま、表、行列見出し、セルだけへ適用します。選択肢は50%、75%、100%、125%、150%、175%、200%で、既定値は100%です。
- 全列配置はすべての列へ一括適用します。列見出しメニューによる個別配置とソートは維持します。
- Google公式Material Symbols Outlinedの既定SVGパスを再確認してローカル同梱します。指定名は `content_paste`、`content_cut`、`content_copy`、`delete`、`undo`、`redo`、`download`、`upload`、`format_align_justify`、`format_align_left`、`format_align_center`、`format_align_right` です。行列操作アイコンは現行のものを維持します。

### CSV・XLSX入出力

- インポートとエクスポートは現在開いている表全体を対象とし、CSVとXLSXだけを扱います。
- インポートはファイルダイアログで選んだデータにより現在の表全体を置き換えます。XLSXに複数シートがある場合はQuick Pickでシートを選択します。
- エクスポートは保存ダイアログでCSVまたはXLSXを選び、現在の表全体を1シートとして出力します。
- CSVはUTF-8として扱い、XLSXとともにセルのMarkdown生文字列を保持します。式の実行、書式、画像、結合セルの再現は対象外で、読み込んだセル値を文字列化します。
- DesktopとWebの双方で動作させるため、ダイアログは `window.showOpenDialog` / `showSaveDialog`、読み書きは `workspace.fs` を使い、Node.jsの `fs` へ依存しません。

## 編集開始と画面構成

- Markdown 文書内で検出した各テーブルの直上に、CodeLens の `GUIで編集` / `Edit in GUI` を表示します。
- CodeLens が無効な場合の代替として、テーブル内のエディターコンテキストメニューにも GUI 編集コマンドを表示します。
- Markdown エディターのコンテキストメニューにクイックテーブル挿入と通常のテーブル挿入コマンドを表示します。
- コマンド ID は `md-table-editor.editTable`、`md-table-editor.quickInsertTable`、`md-table-editor.insertTable` です。コマンドパレットには表示せず、既定キーバインドも設定しません。
- 編集時は現在の Markdown エディターを左グループに保ち、対象テーブルの Webview を直後の右グループへ開きます。
- テーブルごとに独立した Webview パネルを使用します。同じテーブルを再度開く場合は既存パネルを表示し、複数の表は右側グループのタブとして扱います。
- 開いていたパネルは VS Code 再起動後に復元します。対象文書または表を特定できない場合は復元せず通知します。
- ソースカーソルが別の表へ移動しても、その表のパネルを自動的に前面表示しません。

## テーブル挿入

- クイック挿入は確認なしで2列×2行の表を作成します。
- 通常挿入は VS Code の Quick Pickを行数、列数の順に開き、それぞれ1～20から選択させます。行数はヘッダー行を含み、区切り行を含みません。
- 初期セルはすべて空で、外側パイプあり、区切りセルは 3 個のハイフンで作成します。
- 選択範囲とカーソルを保持し、主カーソルがある行の後へ空行を伴って挿入します。既存テーブル内では、その表全体の後へ挿入します。複数カーソル時は主カーソルのみを使用します。
- 挿入後は GUI を直ちに開き、左上のヘッダーセルを編集状態にします。

## Markdown 構文

VS Code 組み込みプレビューと GitHub の双方で表として扱われる GFM Table の共通範囲を対象とします。

- ヘッダー行、区切り行、0 行以上のデータ行を扱います。
- 区切りセルには 3 個以上のハイフンが必要です。
- 外側パイプの有無、空セル、配置用コロン、エスケープされたパイプ、インライン Markdown を扱います。
- fenced code block 内の表らしい文字列は検出しません。
- 4スペースまたはタブで始まるインデントコードは、ヘッダー、区切り行、本文行のいずれとしても表へ取り込みません。0～3スペースの字下げは表として扱います。
- ヘッダー、区切り行の列数が一致しない構文は表として扱いません。
- データ行の列数が不均一な場合は最大列数まで空セルで補います。GUI を開いただけではソースを変更せず、最初の編集確定時に正規化します。
- 外側パイプ、行内空白、改行コード、配置、区切り行のハイフン数を可能な限り保持します。
- GUI で入力した未エスケープの `|` はソースで `\|` に変換します。
- セルは 1 行のみです。改行は入力できません。

## ソース同期とパネルのライフサイクル

- セル編集の確定、構造変更、列幅変更、ソートごとに `WorkspaceEdit` で Markdown ソースへ直ちに反映します。
- 反映後の文書は dirty 状態とし、ディスク保存は通常の Ctrl/Cmd+S に委ねます。
- 各操作は VS Code の Undo 履歴で 1 ステップにまとめます。GUI の Undo/Redo ボタンとキーボード操作も文書の Undo/Redo を利用します。
- ソース側の変更をデバウンスして GUI へ同期します。対象表が削除された、または不正になった場合はパネルを自動的に閉じて通知します。
- Webview からの操作には基準となる文書バージョンを含め、古いバージョンへの更新は拒否して最新状態を再送します。
- 編集中セルはフォーカスを失った時点で確定し、Esc では破棄します。Webview API にはタブを閉じる直前の拒否処理がないため、独自の閉じる確認は表示しません。
- GUI の主セルを選択すると、フォーカスを奪わず対応するソース範囲を表示・選択します。ソース側のカーソル移動も GUI の主セルへ同期します。

## グリッド操作

- 列見出しは A、B、C…、行見出しは 0、1、2、3… とし、Markdown のヘッダーを0行目として列見出しの下へ固定表示します。
- 50 列 × 500 行を保証対象とし、行列の双方向仮想化によって全セルを同時に DOM 化しません。保証値を超える表も切り捨てず編集でき、警告を表示します。
- 単一セル、ドラッグまたは Shift による矩形範囲、Ctrl/Cmd による不連続範囲、行・列見出しによる軸全体の選択に対応します。左上角または Ctrl/Cmd+A で表全体を選択します。
- 単一クリックと範囲ドラッグを区別するため、左ボタンを押してから5px以上移動した場合だけドラッグ選択へ移行します。中ボタンドラッグは選択を変更せず、グリッドを縦横へパンします。
- 不連続範囲のコピーは通知して拒否します。不連続選択中の貼り付けは他の範囲を解除し、主セルを開始位置にします。
- 非編集時は矢印キーで移動、Shift+矢印で範囲拡張、Enter/F2 で既存値の編集、文字入力で置換編集、Delete/Backspace で選択セルをクリア、Ctrl/Cmd+A で表全体を選択します。
- 連続した複数セルの矩形範囲には枠線を表示します。枠線をドラッグするとMarkdown生文字列を矩形のまま移動し、移動元を空にして移動先へ上書きします。重なる範囲は値を退避してから移し、表外へ出る位置では停止します。不連続範囲は移動対象にしません。
- 編集中は矢印キーをキャレット移動に使用し、Enter で確定して同じセルに留まり、Tab/Shift+Tab で確定して隣接セルへ移動し、Esc で破棄します。
- ツールバーから貼り付け、切り取り、コピー、セル内容削除、Undo、Redo、ファイル入出力、ズーム、全列配置、行列の前後追加と削除を実行します。複数行・列の追加数は選択数と同じにします。表の右端にある縦長の `+` から末尾列、下端にある横長の `+` から末尾行を1つ追加できます。
- 未選択の行・列見出しを最初にドラッグしたときは、行なら縦方向、列なら横方向だけの連続範囲を選択します。軸全体が選択済みの見出しを再度ドラッグした場合だけブロックを並べ替え、不連続選択の並べ替えは行いません。ドラッグ中は移動元を半透明にし、ドラッグプレビューと挿入位置ラインを表示します。見出しと対応セルの前半・後半をドロップ対象として、挿入位置を明確に判定します。
- ヘッダー行は先頭に固定し、データ行と入れ替えません。最後の列は削除できません。データ行は 0 行まで削除できます。

## クリップボード、配置、ソート、列幅

- 外部アプリとのコピー、切り取り、貼り付けには TSV を使用します。外部向けコピー値は表示上のプレーンテキストです。
- 拡張機能内では、対応ブラウザーでカスタム MIME のTSVに Markdown の生文字列を保持し、利用できない場合は表示文字列のTSVへフォールバックします。
- ツールバーのコピーと切り取りは単一の連続選択範囲を対象にします。切り取りはクリップボードへの書き込みが成功した後だけ選択セルを空にします。削除ボタンは行列構造を変えず、選択セルの内容だけを空にします。
- 貼り付け範囲が既存表を超える場合は、必要な行列を自動追加します。
- ツールバーから現在の表全体をCSVまたはXLSXとしてインポート・エクスポートします。インポートは表全体を置換し、複数ワークシートのXLSXはQuick Pickで対象シートを選択します。ファイル選択と保存にはVS Code API、読み書きには `workspace.fs` を使います。
- CSV・XLSXの変換はApache-2.0のSheetJS CE 0.20.3を公式CDNの固定URLから導入し、Desktop/Webの拡張バンドルで共有します。CSVはUTF-8 BOM付きで出力し、セル内改行はMarkdownの単一行セルへ合わせて空白へ変換します。
- 列メニューから配置を指定なし、左、中央、右へ変更できます。設定はMarkdownソースだけでなく、GUIの同じ列にあるヘッダーセル、本文セル、編集中の入力欄へ即時反映します。指定なしはGUI上で左寄せとして表示します。
- 単一列の昇順・降順ソートに対応します。ヘッダーは固定し、表示文字列で安定ソートします。すべての非空値を数値として解釈できる場合は数値、それ以外はロケール対応文字列で比較し、空値は常に末尾へ置きます。
- 区切り行のハイフン数を列幅の永続値として扱い、配置用コロンは幅へ数えません。最小値は 3、最大値は設けません。
- 列境界のドラッグは文字幅単位にスナップし、区切り行と各行のパディングを更新します。内容が列幅を超える場合は折り返して行高を広げます。列境界をダブルクリックすると、その列の装飾記号やリンク先URLを除いた表示文字列の最大幅へ自動調整します。
- グリッドのズームは50%、75%、100%、125%、150%、175%、200%から選択します。表、行列見出し、セル、仮想化座標、列幅ドラッグ、選択枠、末尾追加ボタンを同じ倍率で変更し、ツールバー自体は固定します。折り返し文字数は倍率によって変えません。
- 列見出し境界のダブルクリックによる自動幅調整は対象列の表示文字列の最大幅へ合わせます。全角文字や絵文字を考慮し、上限は設定しません。
- Unicode幅計算は結合文字、ZWJ絵文字、国旗、キーパッド絵文字を1書記素として扱います。ASCIIだけの値は高速経路を使用します。

### Webview UI の設計

- 仮想化された列・行・セルは通常のドキュメントフローへ入れず、グリッドキャンバス上へ座標指定で絶対配置します。これにより仮想化対象の要素が縦方向へ積み重なって表示される問題を防ぎます。
- Markdownヘッダー行と本文行の高さは、各セルの推定折り返し数の最大値から算出します。Markdown幅が小さくても、GUIが保証する列の最小ピクセル幅を文字容量へ換算し、表示幅と同じ有効列幅を使って過不足のない行高にします。
- スクロール時はグリッドのスクロール位置を使って、左上の角、列見出し、ヘッダー行、行見出しを表示領域へ追従させます。セル本体は通常どおりスクロールします。
- ツールバーは表示幅1500px以上で名称付き1段、1100～1499pxで同じ順序のアイコンのみ1段、700～1099pxでアイコンのみ2段、699px以下で一部省略した2段にします。アイコン2段では上段を貼り付け系・Undo系・行系・ズーム、下段を配置系・ファイル系・列系とします。最狭幅では上段を貼り付け系・行系、下段をファイルとズームの複合メニュー・配置系・Undo系・列系とします。すべて左寄せでスクロールバーを表示せず、アイコン単体には `title` と `aria-label` を提供します。
- 直接表示の順序は貼り付け、切り取り、コピー、赤色のセル内容削除、Undo、Redo、インポート、エクスポート、ズーム、全列の指定なし・左・中央・右配置、行操作3個、列操作3個です。Google公式の指定名SVGをローカル同梱し、全列配置は `setAllAlignments` の1操作としてVS CodeのUndo履歴へ追加します。
- 行列の追加には `add_row_above`、`add_row_below`、`add_column_left`、`add_column_right`、削除には赤色の `table_rows` と `view_column`、履歴には `undo` と `redo` のMaterial Symbolsを使用します。
- 列メニューは配置・ソートの実行、メニュー外のクリック、グリッドのスクロールで閉じます。開く際は表示中のグリッド領域を計測し、左右端から見切れる分を内側へ移動します。メニューのトリガーにはGoogle公式Material Symbolsの `more_horiz` を使用し、メニュー内の選択済み配置にはチェックを表示します。
- 列メニューを開いている列見出しは、固定された左上角、行番号、ヘッダーセルより高いスタッキング順へ移し、メニュー項目が背面へ隠れないようにします。
- 末尾追加ボタンは仮想化する列・行・セルとは別の静的要素としてグリッドキャンバスの右端と下端へ配置します。列追加ボタンは表本体の全高、行追加ボタンは行見出しを含む表本体の全幅までとし、両者が交差する右下領域には要素を置きません。追加後は新しい末尾セルを選択し、グリッドへフォーカスを戻します。
- アイコンはGoogle公式 `google/material-design-icons` のMaterial Symbols Outlined 24px既定SVG（Weight 400、Fill 0）のパスを `src/webview/icons.tsx` へ同梱します。形状はテーマに依存せず、`currentColor` により通常色とエラー色だけをVS Codeテーマへ追従させます。外部フォントやネットワークへの依存を避け、WebviewのCSPとオフライン動作を維持します。Material SymbolsのライセンスはApache 2.0であり、配布時の表示は `THIRD_PARTY_NOTICES.md` に記録します。
- UI変更時は、選択状態、フォーカスリング、disabled状態、VS Codeテーマ変数、high contrast、reduced motionを維持します。
- 列幅ドラッグ中はWebview全体へ `col-resize` カーソルを固定し、ポインター終了・キャンセル・ウィンドウフォーカス喪失のいずれでも解除します。
- 固定ヘッダーの選択背景は不透明なヘッダー面へ選択色を重ね、下を通過する本文が透けないようにします。表示中セルのMarkdownはブラウザー標準ドラッグを開始させず、既存の複数セル範囲内からでも新しい選択ドラッグを開始できます。
- 行列見出しから開始した範囲選択は、ポインターが隣接するセル側へ外れてもスクロール座標から対象行列を求め、固定軸のまま継続します。同一行列内のポインター移動では選択状態やソース側の表示位置を重複更新しません。
- 行列見出しの選択線、主セル、複数セル選択枠にはテーマのカーソル色を使った3pxの線を表示し、セルの薄い選択背景と区別します。行列見出し面と0行目のタイトル面はわずかに異なる背景色とし、0行目の下端には常時3pxの区切り線を表示します。

## セル表示とセキュリティ

- 非編集時は太字、斜体、取り消し線、コード、リンク、画像などの共通インライン Markdown を装飾表示し、編集時は Markdown 生文字列を表示します。
- raw HTML は実行せず文字列として表示します。
- 通常クリックはセル選択を優先し、Ctrl/Cmd+クリックで安全なリンクを開きます。
- HTTPS およびワークスペース相対の画像だけを制限付きで表示し、それ以外は alt テキストを表示します。
- Webview は nonce 付き CSP、URL スキーム検証、HTML無効化を適用します。
- VS Code の light、dark、high contrast テーマ、スクリーンリーダー、ARIA grid、reduced motion に対応します。

## 初回リリースの対象外

- フィルター
- 数式
- オートフィル
- セル結合
- 複数行セル
- raw HTML の描画・実行
- Marketplace への自動公開
- Marketplace アイコンの作成

## 技術構成

- 拡張機能本体は TypeScript と VS Code API で実装し、Node.js 固有 API を避けて Desktop/Web で共有します。
- Markdown の構文認識には GFM 対応の構文パーサーを使用し、書式保持のためのセル分割と直列化は純粋関数として分離します。
- Webview は React、React DOM、TanStack Virtual を使用したカスタムグリッドです。
- Desktop 用 CommonJS、WebWorker 用 browser エントリ、Webview 用 JavaScript/CSS を esbuild で生成します。
- Webview 状態は `acquireVsCodeApi().getState()` / `setState()` と `WebviewPanelSerializer` で復元します。高いメモリ消費を避けるため `retainContextWhenHidden` へ依存しません。

## ディレクトリ方針

- `src/core`: Markdown 解析、直列化、幅計算、ソートなど VS Code 非依存ロジック
- `src/extension`: コマンド、CodeLens、文書同期、Webview 管理など VS Code 側処理
- `src/webview`: React グリッド、状態管理、Webview メッセージ処理
- `src/test`: 純粋ロジックおよび VS Code 統合テスト
- `dist`: Desktop、Web、Webviewのバンドル生成物。直接編集しません。
- `out`、`out-unit`: Desktop統合テスト、単体テストの生成物。直接編集しません。
- `artifacts`: バージョン付きVSIXなどの配布成果物。Git管理とVSIXへの同梱対象から除外します。
- `scripts`: ビルド・パッケージング用スクリプト。`package-vsix.mjs` は `package.json` のバージョンからVSIX名を生成します。
- `README.md`: GitHubとMarketplaceで既定表示する英語版README。ファイル先頭に日本語で `README.ja.md` への案内リンクを置きます。
- `README.ja.md`: 英語版READMEと同じ利用者・開発者向け情報を保持する日本語版です。
- `RELEASE_CHECKLIST.md`: v1公開前に実環境で確認するユーザー受け入れテスト、重要度、合否基準を記録します。VSIXには同梱しません。

## テストと受け入れ条件

- 外側パイプ、配置、空セル、エスケープ、インラインコード、複数表、コードフェンス、インデントコード、CRLF/CR、不均一行、Unicode幅、不正な操作値、選択範囲、TSV、書式保持を単体テストします。
- 選択、キーボード編集、TSV、行列操作、配置、ソート、列幅、Undo/Redo、安全な Markdown 表示、日英表示、ARIA を UI テストします。
- Desktop統合テストでは公開コマンド、CodeLensの位置と引数、Webviewパネル生成・再利用を確認します。Web統合テストでは公開コマンド、CodeLensの位置と引数、編集コマンドの反復実行を確認します。
- 右クリック、挿入、分割表示、パネル復元、即時同期、dirty状態、保存、Undo、表削除は、実ブラウザーDOM操作とともに今後追加する統合/E2Eテストの対象です。
- `npm run check-types`、`npm run lint`、単体テスト、プロダクションビルド、VSIX生成が成功した状態を完成条件とします。
- ローカル環境で VS Code Extension Host の共有ライブラリが不足する場合は、失敗理由を記録し、GitHub Actions 上で Desktop/Web テストを実行できるようにします。
- v1公開前は `RELEASE_CHECKLIST.md` のユーザー受け入れテストを実施し、P0・P1が0件、P2が修正済みまたは既知の制限として明示済みであることを確認します。実ブラウザーDOMによる操作E2Eが未整備のため、この手動確認を省略しません。

## 現在の実装状態

現在の `0.2.1` では、従来の表解析、CodeLens、Desktop/Web対応、即時ソース同期、仮想化グリッド、TSV、行列操作、配置・ソート・列幅、Undo/Redo、安全なMarkdown表示に加え、2×2クイック挿入、1～20の通常挿入、0始まり行番号、二段階の行列選択・並べ替え、左上角の全選択、枠線によるセル範囲移動、CSV/XLSX入出力、7段階ズーム、全列配置、選択数に応じた行列一括操作、4段階のレスポンシブツールバーを実装済みです。純粋ロジック63件、Desktop統合4件、Web統合3件のテスト、GitHub Actions、VSIX生成、再現可能な性能ベンチマークを用意しています。Marketplaceは未公開であり、v1公開は `RELEASE_CHECKLIST.md` のユーザー受け入れテスト完了待ちです。

### 実装上の補足

- Webviewメッセージ型は `src/shared/protocol.ts` に集約しています。
- Webviewからは表全体ではなく操作を送ることを基本とし、貼り付けなど複数の構造変更が必要な場合だけ置換スナップショットを送ります。
- 列幅リサイズは実際に文字幅が変わった場合だけ `setWidth` を送信します。クリックだけで文書バージョンを進めると、続くダブルクリックの `autoFitColumn` が古い操作として拒否されるためです。
- セル入力へフォーカスして全選択するEffectは編集セルの行・列だけを依存値とし、入力値の更新では再実行しません。入力値を依存させると、1文字ごとに全選択されて次の文字が置換になります。IME変換中のEnterも編集確定として扱いません。
- Webviewからのセル表示要求には世代番号を付け、非同期処理の完了順が前後しても最新要求だけをソースへ反映します。GUIから設定したソース選択と同じ選択変更イベントはWebviewへ返送せず、単一クリックの選択状態を上書きしません。
- Webviewのメッセージ受信Effectではスナップショット、主セル、編集開始処理をref経由で参照し、初回レンダー時の状態へ閉じ込められないようにします。行列追加後のソース選択も最新サイズで判定します。
- GUI操作の楽観的スナップショットは一度だけ計算して状態更新へ再利用します。Unicodeの書記素分割器もモジュール単位で再利用し、行列走査では巨大配列を関数引数へ展開しません。
- 列仮想化とヘッダー・本文行高計算は `columnPixelWidth` を共有します。最小列幅96pxを適用した後の有効文字容量で折り返し数を計算し、Markdown幅3文字をそのまま除数にして行高だけが過剰になる状態を避けます。
- Webview画像はHTTPSまたはワークスペース相対パスだけを表示し、リンクはCtrl/Cmd+クリックかつHTTPSの場合だけVS Codeへ開くよう依頼します。
- TypeScript 6とMarkdownIt 15の宣言ファイル境界に対応するため `skipLibCheck` を有効にしています。プロジェクト自身のソースにはstrict型チェックを継続します。

### 開発コマンド

- `npm run compile`: 型チェック、Lint、Desktop/Web/Webviewビルド
- `npm run test:unit`: VS Code非依存ロジックの単体テスト
- `npm test`: Desktop Extension Host統合テスト
- `npm run test:web`: Web Extension Host統合テスト
- `npm run benchmark`: 単体テスト用JavaScriptを生成し、保証サイズと超過サイズのコア処理を測定
- `npm run package:vsix`: プロダクションビルドと `artifacts/md-table-editor-<version>.vsix` の生成
- 統合テストは開発対象の拡張機能を明示的にactivateしてから、公開コマンドとCodeLens Providerを検証します。Web統合テストは `src/test/webRunner.ts` でMochaブラウザー版のsingletonとテストを単一のCommonJSへバンドルし、Web Extension Hostがサポートする `require('vscode')` だけを外部参照として残します。テスト対象はstable版とし、CLIにローダーを自動選択させます。

### ローカル検証環境

現在の開発コンテナはDesktop/Webテストに必要なGTK、NSS、Chromium系共有ライブラリを標準では備えておらず、通常実行ではExtension HostまたはChromiumの起動前に停止します。2026-08-11の0.2.0検証では、必要なDebianパッケージを `/tmp` へ展開し、システムへインストールせず `LD_LIBRARY_PATH` から参照しました。Desktopは既存のWSLgディスプレイを使って単体を含む67件、Webは既定ポート3000の競合を避けてポート3001のヘッドレスChromiumを使い3件が通過しました。通常の開発環境またはGitHub ActionsではOS依存パッケージを正規に導入して実行します。

### 性能評価

2026-08-11に0.2.0をWSL2、Intel Core i7-12700KF、Node.js 22.23.2で `npm run benchmark` 相当として測定しました。値はウォームアップ後の中央値で、50×500は7標本、100×2000は3標本です。DOM描画、Webviewメッセージ、VS Code文書更新、CSV/XLSX変換は含まず、表解析・直列化・純粋操作だけを測定しています。

| データ | Markdown | 解析 | 直列化 | 1セル更新 | 全列幅調整 | 1列ソート |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ASCII 50列×500行 | 368.0 KiB | 10.42 ms | 5.14 ms | 0.15 ms | 7.80 ms | 0.36 ms |
| Unicode 50列×500行 | 441.1 KiB | 9.62 ms | 140.86 ms | 0.05 ms | 141.40 ms | 0.28 ms |
| Unicode 100列×2000行 | 3520.8 KiB | 75.78 ms | 1147.55 ms | 0.33 ms | 1185.74 ms | 1.24 ms |

保証サイズではASCII表のコア処理はすべて11ms未満です。Unicodeを全セルに含む場合は書記素単位の幅計算が支配的で、保証サイズでも全列幅調整と直列化が約141ms、超過サイズでは約1.15～1.19秒かかります。100列×2000行は保証対象外ですが編集可能とする仕様のため、将来は列単位の幅キャッシュまたは変更列だけを再計算する方式を優先候補とします。

### 既知の改善候補

- 現在の統合テストはコマンド登録、CodeLens、Webviewパネル生成・再利用が中心です。セル編集、ドラッグ、クリップボード、メニュー、テーマ、ARIAを実ブラウザーDOMで操作するE2Eテストは未整備です。
- ソースカーソル移動時は文書全体を複数のリスナーで再解析します。約3.5MiBの測定用文書では解析1回が約64msのため、文書バージョン単位の解析結果キャッシュが改善候補です。
- Unicodeを多用する保証超過表では、直列化と全列幅調整がメインスレッドで1秒を超えます。列幅キャッシュ、差分直列化、Web Workerへの計算分離を検討します。
- 0.2.0のプロダクションバンドルはDesktop拡張511.28KiB、Web拡張511.24KiB、Webview JavaScript365.77KiBで、VSIX全体は475.65KiBです。SheetJSをDesktop/Webへ同梱したため拡張バンドルが増えており、初期表示・初回入出力時間を実機測定したうえでファイル変換部分の遅延読み込みを検討します。

## 設計意図

- CodeLens は表ごとの導線をソース上へ直接配置でき、Webview は VS Code の編集グループ内に GUI を表示できるため採用しました。
- ソースへの即時反映と通常保存を分離することで、VS Code 標準のdirty状態、保存、Undo/Redoと一貫した操作にします。
- DOMベースの仮想化グリッドは、Markdown装飾、テーマ、アクセシビリティを実装しながら 50×500 の性能要件を満たすために採用しました。
- Markdown全体を再整形せず、対象表の書式を保持して更新することで、GUI操作による意図しない大規模差分を防ぎます。
