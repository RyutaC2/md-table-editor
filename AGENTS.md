# md-table-editor AI向け指示書

## 作業開始時の確認

- 最初に `AGENTS_DOCS.md` を読み、リポジトリの現状、実装済み範囲、設計意図を理解してください。
- その後、変更対象のコードと関連する設定・テストを確認してください。
- `AGENTS_DOCS.md` に記載された実装状態と実際のコードが異なる場合は、コードを根拠に仕様書も更新してください。

## このリポジトリについて

このリポジトリは、Markdown ファイルの表を Excel ライクな GUI で編集する VS Code 拡張機能です。初回リリースの要件定義と実装承認は完了しています。実装判断は `AGENTS_DOCS.md` の確定仕様を基準にし、未実装の機能を実装済みであるかのように説明してはいけません。

## 実装方針

- ユーザーが明示的に実装・修正を依頼した場合に、コードを変更してください。質問や「実装したい」という相談では、原則として実装方法の説明にとどめてください。
- TypeScript のソースコードは `src` 配下に配置し、テストは `src/test` 配下に配置してください。
- TypeScript の `strict` 設定を維持し、型安全性を損なうためだけの `any` や型アサーションを追加しないでください。
- VS Code API を利用する処理と、Markdown の解析・変換などの純粋なロジックを分離してください。純粋なロジックは VS Code の実行環境に依存しない形でテストできるようにします。
- 新しい依存関係は、既存の依存関係で代替できないこと、ライセンス・保守性・バンドルサイズに問題がないことを確認してから追加してください。
- `dist` と `out` は生成物です。直接編集せず、ソースコードや設定を変更して生成してください。
- 既存の変更を確認せずに上書きしたり、無関係なファイルを整形したりしないでください。
- 拡張機能本体は Desktop/Web の両方で動作させるため、共有コードで Node.js 固有 API を使用しないでください。
- Markdown の解析、直列化、幅計算、ソートは VS Code API から分離した純粋関数として実装してください。
- Webview と拡張機能間のメッセージは判別可能な union 型で定義し、文書バージョンを検証してください。
- パネルの対象表はURIと開始オフセットで追跡し、ソース変更後に再解析してから書き戻してください。
- Webview では nonce 付き Content Security Policy を設定し、raw HTML、未検証URL、任意スクリプトを実行しないでください。
- Webviewの見た目を変更する場合は、Reactコンポーネントと対応するCSSを同時に確認・更新してください。
- Webviewのアイコンは外部ネットワークや実行時のWebフォントに依存させず、必要なMaterial Symbols由来のSVGをローカル同梱してください。アイコン単体では意味を伝えきれないため、`title` と `aria-label` を併設してください。
- 仮想化グリッドの要素を通常のフローへ戻さないでください。列・行・セルはキャンバス上の座標で配置し、スクロール時の見出し追従とz-indexを確認してください。
- 行列のドラッグ並べ替えを変更する場合は、移動元の半透明表示、ドラッグプレビュー、挿入前後のライン、見出しとセルのドロップ判定を維持してください。
- ツールバーは2段構成を維持し、ツールバー自体へスクロールバーを追加しないでください。アイコンだけへ依存せず、ラベル、`title`、`aria-label` を併用してください。
- Web統合テストは個別テストファイルを直接指定せず、Mochaを含む `dist/web-test/index.js` へバンドルしてください。
- グリッドの保証サイズを維持するため、行列の描画で仮想化を外したり、全セル分のDOMを常時生成したりしないでください。
- GUI操作は、可能な限り VS Code 標準の文書編集、dirty状態、保存、Undo/Redoへ統合してください。

## 要件変更

- `AGENTS_DOCS.md` に記載された初回リリース要件はユーザー承認済みです。実装上の都合だけで機能を削除・変更しないでください。
- 確定仕様を大きく変更する必要がある場合は、根拠、影響範囲、代替案をユーザーへ提示して承認を得てください。
- 小さな実装詳細は、既存仕様と VS Code 標準操作に整合する選択を行い、その判断を `AGENTS_DOCS.md` に記録してください。

## 文書更新

コードや設定を編集するたびに、`README.md`、`AGENTS_DOCS.md`、`AGENTS.md` も内容を確認し、必要な加筆・修正を行ってください。各文書の役割は次のとおりです。

- `README.md`: 利用者・開発者向けの説明。使い方、セットアップ、現在の制限を記載します。
- `AGENTS_DOCS.md`: AI 向けの仕様書。構成、実装済み仕様、開発コマンド、変更理由を記載します。
- `AGENTS.md`: AI 向けの指示書。作業手順、コーディング規約、運用ルールを記載します。

実装予定の内容は、実装済みの内容と区別して記載してください。README の記述は、`package.json` のコマンド名や実際の操作方法と一致させてください。

## 検証

変更後は、変更内容に応じて次のコマンドを実行してください。

```text
npm run check-types
npm run lint
npm run test:unit
npm test
npm run test:web
npm run package
```

小さな文書のみの変更でも、可能な範囲で既存の構成とコマンド記述を確認してください。検証できなかった場合は、理由を最終報告に明記してください。

## 回答方法

- 日本語の敬語で回答してください。
- 実装例のコードを提示する場合は、各処理の役割を順に説明してください。
- 不確かな情報を憶測で断定しないでください。バージョンや外部仕様に依存する内容は、必要に応じて公式情報を確認してください。
- 作業結果は、変更したファイル、主な変更内容、実行した検証を簡潔に報告してください。

## Git 運用

- ユーザーからコミットの指示がない限り、コミットや push は行わないでください。
- コミットの指示がある場合は、変更内容を確認して意図的にコミットし、push まで行ってください。
- コミットメッセージには接頭辞を付けてください。例: `feat: Markdown table編集の基盤を追加`、`fix: 表の解析エラーを修正`、`docs: 開発手順を更新`。
- 変更内容が多い場合のみ、本文に箇条書きの補足を追加してください。README、AGENTS_DOCS、AGENTS の更新内容は、コミットメッセージの本文に記載する必要はありません。
- 大規模な実装は解析、VS Code連携、Webview、テストなど責務単位で細かくコミットし、各コミットを単独で理解できる状態にしてください。
- コミット前に対象差分を確認し、生成物、秘密情報、無関係なユーザー変更を含めないでください。
- ユーザー向け機能を更新するたびに、`package.json` と `package-lock.json` のパッチバージョンを更新し、`CHANGELOG.md` に変更内容を追記してください。初回リリース番号は履歴として保持します。
- `main` とPull Requestでは `.github/workflows/ci.yml` により、Node.js 22でDesktop/Webの統合テストとVSIX生成まで確認してください。
- ローカル統合テストがOS共有ライブラリ不足で実行できない場合は、エラー内容を `AGENTS_DOCS.md` と作業報告へ記録し、単体テストやCIを省略する理由にはしないでください。

例:

```text
feat: 表編集画面の基盤を追加

- Markdown 表を内部データへ変換する処理を追加
- VS Code コマンドから編集画面を開けるように変更
```
