# v1 ユーザー受け入れテスト

`artifacts/markdown-table-gui-0.2.8.vsix` を普段使う VS Code Stableへ新規インストールし、Git管理されたテスト用Markdownで確認してください。自動テストでは保証しにくい操作感と表示を、次の8シナリオに絞って確認します。

## 公開判定

- **P0（公開停止）**: データ消失、別テーブルの破壊、保存不能、クラッシュ、セキュリティ問題
- **P1（公開停止）**: 主要操作を完了できない、Undo不能、入力欠落、一般的な幅・テーマで操作不能
- **P2（原則修正）**: 回避可能だが頻繁に遭遇する操作不良、表示崩れ、分かりにくいフィードバック
- **P3（v1後でも可）**: 操作結果や理解を妨げない軽微な余白・文言・アニメーション

P0・P1が0件で、P2が修正済みまたは既知の制限としてREADMEへ明記済みなら合格です。

## 確認する8シナリオ

- [ ] **導入と発見性**: 日本語・英語で右クリックから2×2クイック挿入と1～20の通常挿入を試し、各表の `テーブルを編集` / `Edit Table` から正しい表が左右分割で開く。20×20も省略されず、タブ名は `table: <ファイル名>` になる。
- [ ] **入力・保存・復旧**: 単一セルのクリックと修飾キーなしの矢印移動で直ちに編集が始まること、ドラッグ・Shift・Ctrl/Cmd選択では始まらないことを確認する。半角英数字、日本語IME、絵文字、`|`、インラインMarkdownを入力し、Enter・Shift+Enter・Tab・Shift+Tabで確定・移動後も編集が続くこと、別セルクリック1回で確定・移動・編集開始できることを確認する。確定直後に左側ソースへ反映され、Ctrl/Cmd+S、GUIとソースのUndo/Redo、再読込後も内容が一致し、別テーブルを書き換えない。
- [ ] **選択・セル移動・クリップボード**: 1クリック、範囲、不連続、行列、左上角の全選択を試す。単一セルと連続範囲は枠線をドラッグして移動でき、表端を越えず、コピー・切り取り・貼り付けで値が欠落しない。下部のセル位置と表サイズも追従する。
- [ ] **行列操作**: ツールバーと末尾の `+` から追加し、複数行列の一括追加・削除を試す。見出しの1回目のドラッグは選択、選択済み見出しの再ドラッグは並べ替えになり、半透明プレビュー・挿入線・結果が一致する。
- [ ] **幅・折り返し・配置**: 列境界ドラッグ、ダブルクリック、列単位と全列の幅調整、長文の自動折り返し、全列・列単位の配置、ソート、50～200%ズームを試す。不要な縦横余白、カーソルのちらつき、ソースとの不一致がない。
- [ ] **レスポンシブUIとテーマ**: 幅約1500・1100・800・550pxで1段／2段／複合メニューへ切り替え、全操作へ到達でき、スクロールバーや見切れがない。Light、Dark、High Contrastで文字、選択枠、メニュー、フォーカスを判別できる。
- [ ] **CSV・XLSX**: 日本語、カンマ、引用符、空セルを含む表をCSV/XLSXへ出力し、再入力して値が一致する。複数シートXLSXでは選択したシートだけが入り、インポート後のUndoで元へ戻せる。
- [ ] **大きな表とWeb**: 50列×500行でスクロール、選択、入力、中ボタンドラッグが実用的に動く。可能なら vscode.dev または github.dev でも検出・編集・保存を確認する。

## UXで重点的に見る点

- 初見で「挿入」「編集」「保存」の導線を迷わず見つけられるか
- 選択範囲、追加・削除対象、ドロップ位置、処理結果が操作前後で明確か
- 入力、スクロール、折り返し、レスポンシブ切替で遅延・跳ね・重なりがないか
- 誤操作をEscまたはUndoで安全に戻せ、ソースとの差分を理解できるか

## 不具合報告に含める情報

重要度、OS・VS Code・拡張機能のバージョン、Desktop/Web、テーマ、表示幅、再現手順、期待結果と実際の結果、操作前後のMarkdown、可能なら画像または短い動画を添えてください。
