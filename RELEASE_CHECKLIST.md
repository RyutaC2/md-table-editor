# v1 ユーザー受け入れテスト

このチェックリストは、`Markdown Grid Editor` を v1 として公開する前に、生成済み VSIX を実際の VS Code へインストールして確認するためのものです。自動テストでは保証しにくい操作感、表示、OS・テーマ差を重点的に確認します。

## 合否基準

- **P0（公開停止）**: データ消失、別テーブルの破壊、保存不能、拡張機能や VS Code のクラッシュ、任意コード実行につながる問題
- **P1（公開停止）**: 主要操作が完了できない、Undo で戻せない、一般的な画面幅・テーマで操作不能、入力が欠落・置換される問題
- **P2（原則修正）**: 回避策はあるが頻繁に遭遇する操作不良、表示崩れ、著しく分かりにくいフィードバック
- **P3（v1後でも可）**: 軽微な余白・文言・アニメーションなど、操作結果や理解を妨げない問題

v1 の公開条件は、P0・P1 が 0 件、P2 が修正済みまたは既知の制限として明示済みであることです。

## 事前準備

- [ ] `artifacts/md-table-editor-0.2.3.vsix` を、普段使っている VS Code Stable へ新規インストールする
- [ ] VS Code と OS のバージョン、表示倍率、使用テーマを記録する
- [ ] Git 管理されたテスト用ワークスペースで確認し、操作前後の差分を比較できるようにする
- [ ] 可能であれば Windows、macOS、Linux のうち2環境以上と、vscode.dev または github.dev でも確認する

## P0/P1: データと基本導線

- [ ] 空のMarkdownで右クリックし、クイック挿入からヘッダー＋データ1行の2列×2行を確認なしで挿入できる
- [ ] 通常挿入で行数、列数の順に1～20を選択し、1×1、3×3、20×20の表を挿入できる
- [ ] 挿入後に左右分割で GUI が開き、左上セルをすぐ編集できる
- [ ] 文書内の各表の直上に `GUIで編集` / `Edit in GUI` が表示され、選んだ表だけが開く
- [ ] 同じ表を再度開いてもパネルが重複せず、別の表は別タブとして開く
- [ ] セル確定直後に左側ソースへ反映され、ファイルは dirty のまま、Ctrl/Cmd+S で保存できる
- [ ] GUI とソースの Undo/Redo が1操作ずつ対応し、保存後・再読込後も内容が一致する
- [ ] GUI表示中に対象表をソースから削除・破壊したとき、別の表を書き換えず安全に閉じる
- [ ] VS Code 再起動後に編集パネルが正しい表へ復元される

## P0/P1: 入力・編集

- [ ] 半角英数字を連続入力しても、1文字ずつ置換されず完全な文字列になる
- [ ] 日本語IMEの変換中に Enter を押しても誤確定せず、変換確定後にセルを確定できる
- [ ] `|`、`\|`、太字、斜体、コード、リンク、絵文字、結合文字を入力してソースとGUIが一致する
- [ ] Enter、Tab、Shift+Tab、Esc、F2、矢印、Delete/Backspace が仕様どおり動作する
- [ ] 1クリックで必ず1セルを選べ、ドラッグ時だけ矩形選択になり、既存の複数セル範囲内からも選択し直せる
- [ ] Shift選択、Ctrl/Cmdによる不連続選択、行全体・列全体・表全体の選択が予測どおりになる
- [ ] 左上角のクリックで表全体を選択でき、Markdownヘッダーが0行目、本文が1行目以降として表示される
- [ ] 連続した複数セル選択に枠線が表示され、枠線のドラッグで値を移動できる
- [ ] セル範囲を重ねて移動しても値が欠落せず、表端へ移動した後は表外へ進まない
- [ ] Excelまたはスプレッドシートとのコピー、切り取り、貼り付けでタブ・行の構造が保持される
- [ ] 表の外へはみ出す貼り付けで必要な行列だけが増え、既存データを意図せず失わない

## P1/P2: 行・列・幅・配置

- [ ] ツールバーと表端の `+` の両方から、前後および末尾へ行列を追加できる
- [ ] 行・列削除とセル内容削除を取り違えにくく、最後の列を削除できないことが分かる
- [ ] 未選択の行・列見出しを最初にドラッグすると固定軸の複数選択になり、選択済み見出しの再ドラッグで並べ替えになる
- [ ] 行・列見出しからセル側へポインターがはみ出しても固定軸の選択が継続し、選択中のカーソルがgrip表示にならない
- [ ] 行・列の並べ替え中に移動元、半透明プレビュー、挿入ラインが表示され、狙った位置へ移動する
- [ ] 複数行・列の前後追加が選択数と同じ数を一括追加し、削除も選択対象を一括処理する
- [ ] 中ボタンドラッグで大きな表を縦横に移動でき、通常選択は変わらない
- [ ] 列境界のドラッグ後も幅が保持され、ダブルクリックでその列だけが内容幅へ調整される
- [ ] `fit_width` が1段の右端、2段のズーム直下、最狭幅の複合メニュー内にあり、全列を内容幅へ調整する
- [ ] 列メニューの幅自動調整が対象列だけを変更する
- [ ] 自動折り返し時に必要な分だけ行高が増え、短い内容の行が過剰に高くならない
- [ ] ツールバーの指定なし・左・中央・右揃えが全列へ、列メニューの配置が対象列だけへ反映され、ソースとGUIが一致する
- [ ] 数値、文字列、空値を含む列の昇順・降順が安定し、ヘッダー行は移動しない

## P1/P2: レスポンシブ表示とメニュー

- [ ] 1400px以上で名称付き1段、1000～1399pxでアイコンのみ1段、600～999pxでアイコンのみ2段、599px以下で複合メニューと配置プルダウンを含む2段になり、すべて左寄せになる
- [ ] 599px以下で下段左の複合メニュー・配置・Undo・Redoが、上段の貼り付け系4ボタンと同じ幅になる
- [ ] どの表示幅でもツールバーにスクロールバーが出ず、すべての操作へ到達できる
- [ ] すべてのアイコンが用途を判別でき、ホバー時の説明とキーボードフォーカスが見える
- [ ] 左端・右端の列メニューが画面外へ見切れず、固定見出しの背面にも隠れない
- [ ] 0行目を選択して下へスクロールしても、固定ヘッダー越しに本文の文字が透けない
- [ ] 行列見出し・主セル・複数セル範囲の太い選択線が背景色と区別でき、0行目と1行目の区切り線が常時見える
- [ ] メニュー項目の選択、メニュー外クリック、グリッドスクロールでメニューが閉じる
- [ ] 通常表示と複合メニュー内のズームが同じ外観で、候補一覧と配置・列メニューがVS Codeの右クリックメニューに近い外観になる
- [ ] 末尾の行追加・列追加ボタンが重ならず、右下が空き、全幅・全高へ正しく伸びる
- [ ] VS Codeのズーム100%、125%、200%で主要操作が隠れず、横・縦スクロールが破綻しない
- [ ] グリッドの50%、75%、100%、125%、150%、175%、200%を切り替え、ツールバーは固定されたまま見出し・セル・選択枠・末尾追加ボタンが同倍率で変わる

## P0/P1: CSV・XLSX入出力

- [ ] UTF-8 CSVをインポートすると現在の表全体だけが置換され、Undoで元へ戻せる
- [ ] カンマ、引用符、日本語、空セルを含む表をCSVへエクスポートして再インポートしても値が一致する
- [ ] 複数ワークシートのXLSXをインポートするとQuick Pickが表示され、選んだ1シートだけが現在の表へ入る
- [ ] XLSXへエクスポートすると `Table` ワークシート1つに現在の表全体が出力される
- [ ] `.csv` と `.xlsx` 以外を選んだ場合は表を変更せず警告し、壊れたファイルでは安全にエラー表示する

## P1/P2: 大きな表・テーマ・Web

- [ ] 50列×500行でスクロール、選択、入力が実用的で、固定見出しやセルに空白・重なりが出ない
- [ ] 100列×2000行では保証外の警告が表示されても、編集と保存を続行できる
- [ ] Light、Dark、High Contrastでツールバー、ズーム、各メニューの背景と文字が同化せず、選択、フォーカス、エラー色を識別できる
- [ ] reduced motion設定時に不要なアニメーションが抑制される
- [ ] キーボードだけで主要操作へ到達でき、スクリーンリーダーでボタン名とセル位置を把握できる
- [ ] vscode.devまたはgithub.devで表の検出、編集、保存がDesktop版と同様に動く

## セキュリティとMarkdown互換性

- [ ] fenced code blockと4スペース／タブのインデントコード内にある表らしい文字列を検出しない
- [ ] 外側パイプなし、空セル、配置指定、不均一な本文行、CRLFの表を編集しても意図しない全体差分が出ない
- [ ] raw HTMLが実行されず文字として表示される
- [ ] 通常クリックではリンクを開かず、Ctrl/Cmd+クリック時だけ安全なHTTPSリンクを開く
- [ ] HTTPや危険なURLの画像・リンクが実行されず、表示が安全にフォールバックする

## UXを重点的に見る観点

- **発見性**: 初見で表の挿入方法とGUIを開く方法が分かるか
- **即時性**: クリック、入力、並べ替え、保存に対する反応が遅れず、結果を見失わないか
- **予測可能性**: 選択、追加位置、削除対象、ドロップ位置が操作前に理解できるか
- **復旧性**: 誤操作をUndoまたはEscで安全に戻せ、ソースとの差異を確認できるか
- **視覚的安定性**: スクロール、折り返し、レスポンシブ切替で要素が跳ねたり重なったりしないか
- **効率性**: マウスとキーボードの双方で、同じ作業をMarkdown手入力より少ない負担で完了できるか
- **アクセシビリティ**: 色だけに依存せず、フォーカス、名称、コントラストが十分か

## 不具合報告に含める情報

- 重要度（P0～P3）
- OS、VS Code、拡張機能のバージョン
- Desktop／vscode.dev／github.devの別、テーマ、ズーム率
- 最小の再現手順、期待結果、実際の結果
- 操作前後のMarkdown、スクリーンショットまたは短い動画
- 毎回再現するか、表の行列数・文字種に依存するか
